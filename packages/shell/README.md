# @honeydrunk/honeyhub-shell

Minimal Tauri-class shell placeholder for HoneyHub.

The full desktop toolkit, installer packaging, code signing, auto-update, and bridge bundling land in later packets. This package exists so the repo scaffold has a stable shell seam from the first commit.
